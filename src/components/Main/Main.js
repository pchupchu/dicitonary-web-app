import SearchForm from "../SearchForm/SearchForm";
import "./Main.css";

function Main({ openPopup }) {
  return (
    <main className="main">
      <SearchForm />
    </main>
  );
}

export default Main;
